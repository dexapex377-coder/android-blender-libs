name: Build OpenEXR for Android

on:
  workflow_dispatch:
  push:
    branches: [main]

env:
  NDK_VERSION: r26d

jobs:
  build-openexr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: ${{ env.NDK_VERSION }}
          add-to-path: false

      - name: Install deps
        run: sudo apt-get install -y ninja-build cmake

      - name: Download OpenEXR source
        run: |
          curl -L https://github.com/AcademySoftwareFoundation/openexr/archive/refs/tags/v3.2.1.tar.gz -o openexr.tar.gz
          tar xzf openexr.tar.gz

      - name: Build OpenEXR for Android
        run: |
          NDK_DIR="${{ steps.setup-ndk.outputs.ndk-path }}"
          TOOLCHAIN="$NDK_DIR/build/cmake/android.toolchain.cmake"
          
          cmake -B build_openexr -S openexr-3.2.1 \
            -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE="$TOOLCHAIN" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-29 \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=ON \
            -DOPENEXR_BUILD_TOOLS=OFF \
            -DOPENEXR_BUILD_TESTS=OFF \
            -DOPENEXR_INSTALL=ON \
            -DCMAKE_INSTALL_PREFIX=install_root
          
          ninja -C build_openexr -j$(nproc)
          ninja -C build_openexr install

      - name: List output
        run: |
          echo "=== IlmThread ==="
          find install_root -name "*IlmThread*" -o -name "*IlmThread*" 2>/dev/null
          echo "=== All libs ==="
          find install_root -name "*.so" -exec ls -lh {} \;

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openexr-android-arm64
          path: install_root/lib/*.so
          retention-days: 30
